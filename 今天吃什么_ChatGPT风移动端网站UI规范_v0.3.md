# 今天吃什么｜ChatGPT 风移动端网站 UI 规范 v0.3

## 1. 视觉定位

关键词：

- 极简
- 安静
- 清楚
- 工具感
- 轻微科技感
- 高留白
- 功能优先
- 移动端优先

整体比例：

- 70% ChatGPT 工具感
- 20% 手机网页易用性
- 10% 抽签趣味

沿用此前确认的小程序视觉方案，但改为移动端响应式网站实现。

---

## 2. 适配范围

### 主要设备

- 手机浏览器
- 微信内置浏览器
- iPhone Safari
- Android Chrome

### 布局宽度

- 最小适配：320px
- 主要设计宽度：375px–430px
- 手机页面内容最大宽度：480px
- 桌面端内容最大宽度：520px，居中显示

桌面端不扩展成复杂双栏后台，保持手机卡片式布局。

---

## 3. 色彩规范

| 用途 | 色值 |
|---|---|
| 页面背景 | `#F7F7F5` |
| 卡片背景 | `#FFFFFF` |
| 主文字 | `#1F1F1F` |
| 次文字 | `#6B6F76` |
| 弱提示文字 | `#9A9A95` |
| 边框 / 分割线 | `#E8E8E3` |
| 主强调色 | `#2F6F5F` |
| 浅强调背景 | `#E4F0EB` |
| 危险 / 删除 | `#C84B4B` |

规则：

- 中性色约占 85%
- 绿色只用于主要操作、选中状态和少量提示
- 不使用大面积渐变
- 不使用外卖平台常见的高饱和黄、橙、蓝
- 不使用发光、彩纸、花草装饰

---

## 4. 字体与尺寸

使用系统字体栈：

```css
font-family:
  -apple-system,
  BlinkMacSystemFont,
  "Segoe UI",
  "PingFang SC",
  "Hiragino Sans GB",
  "Microsoft YaHei",
  sans-serif;
```

| 用途 | 字号 | 字重 |
|---|---:|---|
| 页面主标题 | 28–30px | 600 |
| 结果店名 | 28–32px | 600 |
| 区块标题 | 18–20px | 600 |
| 正文 | 16px | 400 |
| 辅助信息 | 14px | 400 |
| 按钮文字 | 16–17px | 500 |
| 底部导航 | 11–12px | 400 |

触控目标最小高度建议 44px。

---

## 5. 圆角与阴影

| 组件 | 圆角 |
|---|---:|
| 主要卡片 | 16px |
| 输入框 | 12px |
| 主按钮 | 14px |
| 二段选择器 | 12px |
| 店名首字头像 | 10px |

卡片使用：

- 白色背景
- 1px 浅灰边框
- 极轻阴影

```css
box-shadow: 0 2px 12px rgba(0, 0, 0, 0.04);
```

---

## 6. 图标规范

统一使用：

- 线性图标
- 1.5–2px 线宽
- 圆角端点
- 单色或低饱和色

禁止：

- 食物图片或插画
- 用户上传图片
- 花草、叶子
- 卡通表情
- 含义不明的星星、爱心、笑脸

店铺使用“店名首字头像”：

- 40 × 40px
- 圆角方形
- 浅色背景
- 深色文字

底色循环：

- `#E4F0EB`
- `#ECECEA`
- `#EEEAF4`

---

## 7. 全站结构

### 顶部区域

移动端采用简洁顶部栏：

- 左侧可显示页面标题
- 右侧仅放必要操作
- 不做复杂导航菜单

### 底部导航

登录后的主要页面使用固定底部导航：

```text
首页 ｜ 记录 ｜ 历史 ｜ 设置
```

样式：

- 白色背景
- 顶部极浅分割线
- 当前项深绿色
- 未选中灰色
- 适配安全区 `env(safe-area-inset-bottom)`

桌面端可继续显示底部导航，保持体验一致。

---

## 8. 登录页

结构：

```text
今天吃什么

从吃过的店里，
帮你快点做决定。

[ 手机号或邮箱 ]
[ 获取验证码 ]

[ 登录 / 注册 ]

隐私政策 ｜ 用户协议
```

要求：

- 单列布局
- 不要求头像、昵称、性别等资料
- 登录卡片居中
- 不使用宣传轮播图
- 错误提示放在对应输入框附近
- 加载时禁用重复提交

---

## 9. 首页

结构：

```text
今天吃什么
从吃过的店里，帮你快点做决定

[ 正餐 | 宵夜 ]

现在要抽：正餐

[ 吃啥呢 ]
点一下抽一个

记录今天吃了什么
```

### 正餐 / 宵夜选择器

采用二段式 segmented control：

- 默认选中“正餐”
- 未选中：透明或浅灰背景、深灰文字
- 选中：`#E4F0EB` 背景、深绿色文字
- 切换后同步更新当前抽选类型提示

### 主操作卡片

- 白色大型操作卡片
- 高度约 180–220px
- 简洁抽签线性图标
- 主文案“吃啥呢”
- 辅助文案“点一下抽一个”
- 整张卡片可点击

不使用发光圆球、多层渐变、彩色粒子或食物图片。

### 空状态

当前类型无记录：

```text
还没有记录过宵夜

先记下几家吃过的宵夜，
之后就能帮你抽。

[ 记录一家 ]
```

无合格候选：

```text
最近这类吃得有点勤

当前没有达到间隔条件的店。

[ 临时放宽一次 ]
[ 去记录新店 ]
```

---

## 10. 抽取中状态

```text
正在帮你挑正餐
稍等一下
```

或：

```text
正在帮你挑宵夜
稍等一下
```

使用轻微缩放和三点加载，持续约 300–600ms。

---

## 11. 结果页

正餐：

```text
这顿正餐吃这个

[ 老 ]

老张黄焖鸡
黄焖鸡米饭

你已经 12 顿没吃这家了

[ 就它了 ]
[ 换一个 ]
```

宵夜：

```text
今晚宵夜吃这个

[ 陈 ]

陈记烧烤

你已经 8 顿宵夜没吃这家了

[ 就它了 ]
[ 换一个 ]
```

规则：

- 白色结果卡片
- 店名首字头像
- 菜名有则显示，无则不占位
- 类型文案必须明确
- 不显示食物图片
- 不使用庆祝彩纸

---

## 12. 记录外卖页

```text
记录外卖
记下今天吃了什么

店名
[ 请输入店名 ]

菜名（选填）
[ 例如：鸡腿饭 ]

吃饭日期
[ 2026-07-24 ]

用餐类型
[ 正餐 | 宵夜 ]

[ 保存记录 ]
```

要求：

- 输入框使用浏览器原生能力但统一视觉
- 日期优先使用适合移动端的日期选择器
- 用餐类型为独立必填字段
- 保存按钮在手机上易于单手点击
- 键盘弹出时不能遮挡当前输入区域

---

## 13. 历史记录页

按日期分组：

```text
7月24日

[老] 老张黄焖鸡
     黄焖鸡米饭      正餐

[陈] 陈记烧烤
                      宵夜
```

规则：

- 右侧显示低强调度的“正餐 / 宵夜”标签
- 不显示“多少顿没吃”
- 点击整行进入编辑
- 删除操作必须二次确认
- 长列表使用分页、加载更多或虚拟化中的简单方案，第一版优先分页或加载更多

---

## 14. 设置页

仅包含：

- 候选间隔顿数
- 退出登录
- 隐私政策
- 用户协议
- 删除账户

不增加昵称、头像、主题皮肤等无关设置。

---

## 15. 网络状态与反馈

必须有：

- 页面初次加载骨架或简洁 loading
- 网络错误提示
- 保存中状态
- 保存成功提示
- 删除中状态
- 防止重复提交
- 请求失败后允许重试

提示文案简洁，不使用系统弹窗堆叠。

---

## 16. 间距规范

以 8px 为基础网格：

| 场景 | 间距 |
|---|---:|
| 页面左右边距 | 20px |
| 标题与副标题 | 8px |
| 副标题与主内容 | 28–32px |
| 卡片内部边距 | 20px |
| 表单字段之间 | 20px |
| 按钮之间 | 12px |
| 列表分组之间 | 24px |
| 底部导航预留 | 72–88px |

---

## 17. 文案规范

使用：

- 吃啥呢
- 点一下抽一个
- 现在要抽：正餐
- 现在要抽：宵夜
- 正在帮你挑正餐
- 正在帮你挑宵夜
- 这顿正餐吃这个
- 今晚宵夜吃这个
- 就它了
- 换一个
- 记录今天吃了什么

禁止营销文案。

---

## 18. 网站执行约束

- 不擅自增加食物图片或插画
- 首页不得一进入就展示结果
- 首页必须先选择正餐 / 宵夜
- 记录页必须有独立的“用餐类型”
- 结果页必须明确显示当前抽取类型
- 页面必须移动端优先
- 不得只用浏览器本地存储保存历史数据
- 所有组件保持清楚、安静、工具化
