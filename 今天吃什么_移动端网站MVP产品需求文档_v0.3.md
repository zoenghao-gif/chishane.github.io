# 今天吃什么｜移动端网站 MVP 产品需求文档 v0.3

## 1. 产品定位

帮助每天点外卖但不知道吃什么的人，从自己的历史外卖记录中快速做决定。

### 产品形态

- 移动端优先的网站
- 用户主要通过手机浏览器或微信内置浏览器访问
- 页面同时兼容桌面浏览器，但桌面端不是第一优先级
- 后续可选升级为 PWA，但不作为第一版必须功能

### 核心原则

- 只解决“今天吃什么”
- 不做另一个外卖平台
- 不收集食物图片
- 不要求用户维护复杂资料
- 历史记录必须支持长期保存
- 先完成可用 MVP，再根据真实使用反馈迭代

---

## 2. 目标用户

- 日常不想自己做饭
- 经常依赖外卖
- 饭点前反复浏览外卖平台，但仍然难以决定
- 已经有一些吃过或爱吃的店，但一时想不起来
- 习惯通过手机浏览器使用轻量工具

---

## 3. 核心使用流程

### 3.1 首次访问

用户打开网站后：

1. 可以先查看产品说明
2. 进入使用前完成登录或注册
3. 登录后进入个人外卖数据空间
4. 若无数据，进入空状态并引导记录第一家店

### 3.2 用户账户

为了长期保存历史数据，正式版本必须有用户身份体系。

第一版可选方案：

- 手机号验证码登录
- 邮箱验证码登录
- 其他低摩擦登录方式

具体方式由开发方案决定，但必须满足：

- 不同用户数据隔离
- 用户换手机后能够找回历史数据
- 用户重新登录后数据仍然存在
- 登录状态可在合理期限内保持

游客模式不是第一版必需功能。若实现游客模式，必须明确说明游客数据可能只保存在本机，并提供升级账户的迁移方案。

### 3.3 建立个人外卖库

每条记录包含：

- 店名：必填
- 菜名：选填
- 吃饭日期：必填
- 用餐类型：必填，仅限“正餐”或“宵夜”

不保存：

- 图片
- 价格
- 用户手动评分
- 备注
- 标签
- 店铺类型

### 3.4 设置候选规则

用户设置“连续多少顿没有吃某家店后，该店可以重新进入候选库”。

规则按“顿数”计算，不按自然日计算。

### 3.5 首页选择抽选类型

首页必须先让用户选择：

- 正餐
- 宵夜

默认选中“正餐”，并明确提示当前将抽取哪一类。

### 3.6 抽选

1. 根据用户选择筛选对应的用餐类型
2. 排除尚未达到间隔顿数的店
3. 排除本轮已经被用户换掉的结果
4. 从剩余候选中随机抽取 1 个
5. 进入结果页

### 3.7 结果反馈

结果页显示：

- 当前类型
- 店名
- 菜名（有则显示）
- 距离上次吃该店已经过去多少顿

操作：

- 就它了
- 换一个

点击“就它了”：

- 确认本次选择
- 生成一条新的吃饭记录
- 用餐类型继承本次抽选类型
- 吃饭日期默认当天

点击“换一个”：

- 本轮不再抽到当前结果
- 从剩余候选中继续抽取

若本轮候选全部被换掉：

- 提示“当前候选都看过了”
- 允许重新开始本轮
- 或引导用户记录新店

---

## 4. MVP 页面范围

### 4.1 登录 / 注册页

包含：

- 产品名称
- 一句话说明
- 登录或注册入口
- 隐私政策和用户协议入口

要求：

- 移动端单手可操作
- 登录步骤尽量少
- 不展示营销横幅
- 不强迫用户填写昵称、头像等无关资料

### 4.2 首页

包含：

- 标题“今天吃什么”
- 简短说明
- 正餐 / 宵夜切换控件
- 当前抽选类型提示
- 主操作“吃啥呢”
- “记录今天吃了什么”入口
- 底部导航

首次打开不得直接展示推荐结果。

### 4.3 抽取中状态

显示当前正在抽“正餐”或“宵夜”，使用简单加载动效。

### 4.4 结果页

显示：

- “这顿正餐吃这个”或“今晚宵夜吃这个”
- 店名首字头像
- 店名
- 菜名
- 间隔顿数
- 就它了
- 换一个

### 4.5 记录外卖页

字段：

- 店名
- 菜名（选填）
- 吃饭日期
- 用餐类型：正餐 / 宵夜
- 保存记录

### 4.6 历史记录页

按日期分组展示：

- 店名首字头像
- 店名
- 菜名
- 用餐类型标签
- 吃饭日期

支持：

- 编辑
- 删除
- 删除二次确认

### 4.7 设置页

第一版仅包含：

- 候选间隔顿数
- 退出登录
- 隐私政策
- 用户协议
- 删除账户入口（如上线对外提供服务，必须考虑）

不要增加复杂个人资料页。

### 4.8 必要状态

- 未登录
- 登录失败
- 网络异常
- 外卖库为空
- 当前类型没有记录
- 当前类型没有符合间隔条件的候选
- 本轮候选已经全部换过
- 保存成功
- 删除确认
- 数据加载中
- 数据加载失败

---

## 5. 数据结构

### 5.1 用户表 `users`

| 字段 | 类型建议 | 说明 |
|---|---|---|
| id | string / uuid | 用户唯一编号 |
| login_identifier | string | 手机号、邮箱或第三方账号标识 |
| created_at | datetime | 注册时间 |
| updated_at | datetime | 最近更新时间 |

界面不需要展示用户编号。

### 5.2 外卖记录表 `meal_records`

| 字段 | 类型建议 | 说明 |
|---|---|---|
| id | string / uuid | 记录唯一编号 |
| user_id | string / uuid | 所属用户 |
| shop_name | string | 店名，必填 |
| food_name | string / null | 菜名，选填 |
| eat_date | date | 吃饭日期 |
| meal_type | enum | `main_meal` 或 `late_night` |
| created_at | datetime | 记录创建时间 |
| updated_at | datetime | 最近更新时间 |

### 5.3 用户设置表 `user_settings`

| 字段 | 类型建议 | 说明 |
|---|---|---|
| user_id | string / uuid | 所属用户 |
| required_gap_meals | number | 重新进入候选库所需间隔顿数 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 最近更新时间 |

### 5.4 本轮抽选状态

第一版可保存在浏览器会话状态中，不必长期入库：

- 当前抽选类型
- 本轮已经出现过的店
- 当前结果

刷新页面后允许重新开始一轮。

---

## 6. 候选与抽选规则

### 6.1 店铺合并

- 候选对象以店名为主
- 同一用户下，同一店名的多条记录视为同一家店
- 菜名仅用于辅助展示
- 若同一家店记录过多个菜名，结果页可展示最近一次填写的菜名

第一版不做复杂的店名纠错或自动合并。

### 6.2 类型筛选

- 抽正餐时，只处理 `main_meal`
- 抽宵夜时，只处理 `late_night`
- 同一家店可同时存在于正餐和宵夜库中，两类记录分别计算

### 6.3 间隔顿数

只计算同一用户、同一用餐类型下的后续记录。

例如：

某店上次作为正餐吃过，此后记录了 5 顿正餐和 2 顿宵夜，则正餐间隔为 5 顿，宵夜记录不参与正餐间隔。

### 6.4 随机规则

第一版在合格候选中均匀随机，不做复杂推荐评分。

---

## 7. 数据保存与安全要求

- 历史记录必须保存在服务端数据库
- 浏览器本地存储只能保存临时状态或非关键偏好
- 不得只依赖 localStorage 保存用户历史
- 所有查询和修改必须按当前用户隔离
- 后端必须验证用户身份，不得只依赖前端传入的 user_id
- 数据传输使用 HTTPS
- 密码若存在，不得明文保存
- 第一版尽量采用验证码或第三方认证，降低密码管理风险
- 上线前准备隐私政策和用户协议
- 需要提供数据导出与账户删除的后续扩展空间

---

## 8. 网站技术要求

- 移动端优先响应式布局
- 主要适配宽度：320px–480px
- 同时保证桌面浏览器基本可用
- 支持微信内置浏览器、Chrome、Safari 等常见移动浏览器
- 页面首次加载尽量轻
- 不使用大图、视频或重型动画
- 前后端技术由 Work 提出方案后确认
- 数据库、认证和部署方案必须说明预计费用
- 未经用户确认，不开通付费资源

---

## 9. 明确不做

第一版禁止增加：

- 美团、饿了么接口
- 自动读取订单
- OCR 或截图识别
- 食物图片
- 用户上传图片
- 附近商家
- 优惠信息
- AI 聊天推荐
- 营养分析
- 社交分享
- 社区
- 排行榜
- 手动喜欢程度
- 复杂推荐评分
- 早餐、午餐、晚餐进一步细分
- 原生 App
- 微信小程序
- 桌面端专属复杂布局

---

## 10. 成功标准

验证：

- 用户是否愿意持续记录
- 用户是否会在饭点打开网站
- 抽选是否减少选择时间
- 正餐 / 宵夜区分是否符合习惯
- “换一个”的次数是否合理
- 用户是否能理解“间隔多少顿”
- 登录和历史恢复是否稳定
- 手机浏览器操作是否顺手

---

## 11. 开发约束

- 优先完成可通过浏览器访问的部署版本
- 不擅自增加功能
- 页面和数据逻辑严格按照本文档
- 每完成一个阶段先自测，再进入下一阶段
- 先输出方案，确认后再大量写代码
- 所有可能产生费用的服务，必须先列明再开通
